'use client';
import variables from '@/app/variables.module.scss';
const theme = {
	colors: {
		primary: variables.primaryColor,
		secondary: variables.secondaryColor,
		background: variables.backgroundColor,
		outline: variables.outlineColor
	}
};

export default theme;
