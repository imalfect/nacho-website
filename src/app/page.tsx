import Home from '@/components/Home';
import { metadata as metadataImported } from '@/config/metadata';
export const metadata = metadataImported;
export default function HomePage() {
	return <Home />;
}
